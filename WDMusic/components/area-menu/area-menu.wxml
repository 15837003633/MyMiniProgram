<area-header title="{{title}}" hasMore="{{hasMore}}" bind:onMoreClick="onSongMenuMoreClick"></area-header>
<scroll-view class="list" scroll-x enable-flex>
  <block wx:for="{{menuList}}" wx:key="id">
    <menu-item class="item" menuInfo="{{item}}"/>
  </block>
</scroll-view>
